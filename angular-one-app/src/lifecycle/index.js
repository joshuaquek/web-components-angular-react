import { onAdopted as adopted } from './on_adopted'
import { onConnected as connected } from './on_connected'
import { onChange as changed } from './on_change'
import { onDisconnected as disconnected } from './on_disconnected'

export const onAdopted = adopted
export const onConnected = connected
export const onChange = changed
export const onDisconnected = disconnected
